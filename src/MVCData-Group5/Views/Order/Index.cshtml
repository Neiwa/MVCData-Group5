
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm("ViewForCustomer", "Order", FormMethod.Get))
{
    @Html.DropDownList("email", (SelectList)ViewBag.CustomerSL)
    <button type="submit" class="btn">View</button>
}
